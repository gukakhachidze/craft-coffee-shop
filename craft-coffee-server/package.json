{
  "name": "craft-coffee-server",
  "version": "1.0.0",
  "description": "JSON Server for Craft Coffee Shop",
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
